'use strict';

module.exports = {
    zip: jest.fn(function(sourceDir, destFile, opts) {

    }),
    unzip: jest.fn(function(sourceFile, destDir) {

    }),
    tar: jest.fn(function(sourceDir, destFile, opts) {

    }),
    untar: jest.fn(function(sourceFile, destDir) {

    })
};